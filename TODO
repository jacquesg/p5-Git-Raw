# Classes:
* attr
* note
* odb
* packbuilder
* reflog
* submodule
* transport
* treebuilder

# Methods
* Reference -> foreach(...) (replaces Branch -> foreach?)
* Repository -> state
* Repository -> merge(...)
