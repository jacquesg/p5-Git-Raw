# Classes:
* attr
* note
* odb
* packbuilder
* reflog
* submodule
* transport
* treebuilder

# Methods (other)
* Branch -> is_head
* Config -> foreach(...)
* Config -> refresh
* Index -> remove(...)
* Reference -> foreach(...) (replaces Branch -> foreach?)
* Remote -> foreach(...) (+ update $repo -> remotes)
* Repository -> state
* Repository -> checkout(...)
* Repository -> merge(...)
