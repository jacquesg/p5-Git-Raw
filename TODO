# Classes:
* attr
* note
* odb
* packbuilder
* push
* reflog
* submodule
* transport
* treebuilder

# Methods
* Index -> remove(...)
* Reference -> foreach(...) (replaces Branch -> foreach?)
* Repository -> state
* Repository -> checkout(...)
* Repository -> merge(...)
