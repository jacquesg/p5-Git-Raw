# Classes:
* attr
* note
* odb
* packbuilder
* reflog
* submodule
* transport
* treebuilder

# Methods (other)
* Blob -> create($buf) (+ $repo -> blob(...))
* Branch -> move($name)
* Branch -> is_head
* Config -> foreach(...)
* Config -> refresh
* Index -> remove(...)
* Reference -> foreach(...) (replaces Branch -> foreach?)
* Reference -> is_{branch,packed,remote}
* Remote -> foreach(...) (+ update $repo -> remotes)
* Repository -> state
* Repository -> checkout(...)
* Repository -> merge(...)
* Tag -> foreach (+ update $repo -> tags)
