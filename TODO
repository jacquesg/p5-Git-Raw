# Classes:
* attr
* note
* odb
* packbuilder
* push
* reflog
* submodule
* transport
* treebuilder

# Methods
* Branch -> is_head
* Config -> foreach(...)
* Config -> refresh
* Index -> remove(...)
* Reference -> foreach(...) (replaces Branch -> foreach?)
* Repository -> state
* Repository -> checkout(...)
* Repository -> merge(...)
