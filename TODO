# Classes:
* attr
* note
* odb
* packbuilder
* push
* reflog
* submodule
* transport
* treebuilder

# Methods
* Reference -> foreach(...) (replaces Branch -> foreach?)
* User-defined checkout strategies for Repository -> clone() and checkout()
* Repository -> state
* Repository -> merge(...)
