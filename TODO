# Classes:
* attr
* note
* odb
* packbuilder
* push
* reflog
* submodule
* transport
* treebuilder

# Methods
* Reference -> foreach(...) (replaces Branch -> foreach?)
* Repository -> state
* Repository -> merge(...)
